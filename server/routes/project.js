import express from 'express'
import db from '../db/connection.js'

const router = express.Router()

// TODO: Create project routes and table in supabase

router.get('/', async (req, res) => {
    
})

router.route('/:id')
  .get(async (req, res) => {
    
  })
  .post(async (req, res) => {
    
  })
  .patch(async (req, res) => {
    
  })
  .delete(async (req, res) => {
    
  })

export default router